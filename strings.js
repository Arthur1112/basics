// let fruits = ['apple', 'banana', 'orange']; // array 

// console.log(fruits.length) // 3

// console.log(fruits[1].length) // 6 from banana

// console.log(fruits[1][0]) // b from banana

// console.log(fruits) // the array

// console.log(fruits.toString()) // the array as a string

// console.log(fruits.join(', ')) // adds a comma and a space

const delivery = 'kiwi|grape|watermelon|cantaloupe'

const produce = delivery.split('|') // splits it from the | into an array
console.log(produce)

console.log(delivery.includes('watermelon')) // checks to see if watermelon is in the array

